name: validate

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  posix:
    env:
      # Populate this repository variable with the immutable Agent Utilities
      # 0.5.10 release commit after that dependency ships. The explicit empty
      # check prevents default-branch drift from becoming a compatibility pin.
      AGENT_UTILITIES_COMPAT_REF: ${{ vars.AGENT_UTILITIES_0_5_10_RELEASE_SHA }}
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
      - name: Require immutable Agent Utilities compatibility pin
        shell: bash
        run: printf '%s\n' "$AGENT_UTILITIES_COMPAT_REF" | grep -Eq '^[0-9a-f]{40}$'
      - name: Checkout compatible Agent Utilities
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          repository: novotnyllc/agent-utilities
          ref: ${{ env.AGENT_UTILITIES_COMPAT_REF }}
          path: .compat/agent-utilities
      - name: Install OpenSSH server for scope validation
        if: runner.os == 'Linux'
        run: sudo apt-get update && sudo apt-get install -y --no-install-recommends openssh-server
      - name: Validate Bash and integrity
        shell: bash
        run: |
          set -eu
          bash -n plugins/machine-utilities/scripts/machine-utilities
          bash -n plugins/machine-utilities/scripts/certify-ssh-node
          bash -n plugins/machine-utilities/scripts/collect-posix
          bash -n plugins/machine-utilities/scripts/enroll-privilege-posix
          bash -n plugins/machine-utilities/scripts/enroll-ssh-posix
          bash -n plugins/machine-utilities/scripts/prepare-ssh-identity
          bash -n plugins/machine-utilities/scripts/privilege-broker-posix
          bash -n plugins/machine-utilities/scripts/test-machine-utilities
          bash -n plugins/machine-utilities/scripts/update-integrity
          if [[ "$RUNNER_OS" == Linux ]]; then
            shellcheck --severity=warning plugins/machine-utilities/scripts/machine-utilities \
              plugins/machine-utilities/scripts/collect-posix \
              plugins/machine-utilities/scripts/test-machine-utilities \
              plugins/machine-utilities/scripts/update-integrity
          fi
          plugins/machine-utilities/scripts/update-integrity
          git diff --exit-code -- plugins/machine-utilities/integrity.json
      - name: Validate public identity and transport helpers
        shell: bash
        run: |
          set -eu
          plugins/machine-utilities/scripts/enroll-ssh-posix self-test
          plugins/machine-utilities/scripts/prepare-ssh-identity self-test
          plugins/machine-utilities/scripts/certify-ssh-node self-test
      - name: Validate fleet skill frontmatter
        shell: bash
        run: |
          set -eu
          python3 - plugins/machine-utilities/skills/*/SKILL.md <<'PY'
          import pathlib
          import sys

          for argument in sys.argv[1:]:
              path = pathlib.Path(argument)
              lines = path.read_text(encoding="utf-8").splitlines()
              if not lines or lines[0] != "---" or "---" not in lines[1:]:
                  raise SystemExit(f"invalid frontmatter boundary: {path}")
              end = lines.index("---", 1)
              fields = {}
              for line in lines[1:end]:
                  if ": " not in line:
                      raise SystemExit(f"invalid frontmatter field: {path}: {line}")
                  key, value = line.split(": ", 1)
                  if key in fields or not value:
                      raise SystemExit(f"invalid frontmatter value: {path}: {key}")
                  fields[key] = value
              if set(fields) != {"name", "description"} or fields["name"] != path.parent.name:
                  raise SystemExit(f"invalid skill identity: {path}")
          PY
      - name: Run fixture suite
        shell: bash
        env:
          AGENT_UTILITIES_MODEL_ROUTER: ${{ github.workspace }}/.compat/agent-utilities/plugins/agent-utilities/scripts/model-routing.mjs
          AGENT_UTILITIES_PLUGIN_MANIFEST: ${{ github.workspace }}/.compat/agent-utilities/plugins/agent-utilities/.codex-plugin/plugin.json
        run: |
          chmod -R go-w plugins/machine-utilities
          plugins/machine-utilities/scripts/test-machine-utilities

  windows:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
      - name: Validate PowerShell native boundaries and release hashes
        shell: pwsh
        run: |
          $ErrorActionPreference = "Stop"
          $scripts = @(
            "plugins/machine-utilities/scripts/collect-windows.ps1",
            "plugins/machine-utilities/scripts/apply-windows.ps1",
            "plugins/machine-utilities/scripts/enroll-privilege-windows.ps1",
            "plugins/machine-utilities/scripts/enroll-windows-sftp.ps1",
            "plugins/machine-utilities/scripts/privilege-broker-windows.ps1",
            "plugins/machine-utilities/scripts/profile-worker-windows.ps1",
            "plugins/machine-utilities/scripts/register-profile-task-windows.ps1",
            "plugins/machine-utilities/scripts/windows-native-canary-runner.ps1"
          )
          foreach ($script in $scripts) {
            $tokens = $null
            $errors = $null
            [void][System.Management.Automation.Language.Parser]::ParseFile(
              (Resolve-Path $script), [ref]$tokens, [ref]$errors
            )
            if ($errors.Count -gt 0) { throw ($errors -join "`n") }
          }
          & pwsh -NoLogo -NoProfile -NonInteractive -File `
            "plugins/machine-utilities/scripts/apply-windows.ps1" -SelfTest
          if ($LASTEXITCODE -ne 0) { throw "Windows native boundary self-test failed" }
          foreach ($script in @($scripts | Where-Object { $_ -notlike "*apply-windows.ps1" })) {
            & pwsh -NoLogo -NoProfile -NonInteractive -File $script -SelfTest
            if ($LASTEXITCODE -ne 0) { throw "Windows self-test failed: $script" }
          }
          $root = Resolve-Path "plugins/machine-utilities"
          $integrity = Get-Content "$root/integrity.json" -Raw | ConvertFrom-Json
          foreach ($file in @($integrity.files)) {
            $actual = (Get-FileHash (Join-Path $root $file.path) -Algorithm SHA256).Hash.ToLowerInvariant()
            if ($actual -ne $file.sha256) { throw "Integrity mismatch: $($file.path)" }
          }
          $requirementPath = Join-Path $env:RUNNER_TEMP "machine-utilities-executor.json"
          try {
            [ordered]@{
              schema = "machine-utilities.executor"
              schema_version = 1
              plugin = $integrity.plugin
              marketplace = $integrity.marketplace
              version = $integrity.version
              integrity_manifest_sha256 = (
                Get-FileHash (Join-Path $root "integrity.json") -Algorithm SHA256
              ).Hash.ToLowerInvariant()
              files = @($integrity.files)
            } | ConvertTo-Json -Depth 20 | Set-Content -LiteralPath $requirementPath -Encoding utf8NoBOM
            $verified = & pwsh -NoLogo -NoProfile -NonInteractive -File `
              "plugins/machine-utilities/scripts/apply-windows.ps1" `
              -VerifyExecutor -ExecutorRequirementPath $requirementPath |
              ConvertFrom-Json
            if ($LASTEXITCODE -ne 0 -or $verified.verified -ne $true) {
              throw "Native executor verification failed"
            }
          } finally {
            Remove-Item -LiteralPath $requirementPath -Force -ErrorAction SilentlyContinue
          }
